{
  "name": "@f/normalize-bowser",
  "description": "Normalize a bowser object to a more usable form",
  "repository": "git://github.com/micro-js/normalize-bowser.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "bowser": "^1.4.5",
    "hihat": "^2.5.0",
    "tape": "^4.2.2"
  }
}
